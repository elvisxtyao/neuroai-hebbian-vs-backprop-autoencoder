# v1.0-final Release Preflight Audit

Audit date: 2026-08-11

Preflight source commit: `601419db6fc68854967d63fd6c6980e554dd9d27`

License-resolution base commit: `fa868f2968bc5a26624dc5a72ff2e61c00527e0f`

Release branch: `codex/final-release`

Decision: **RELEASE BLOCKED**

This audit covers release governance, repository provenance, Git/GitHub state,
and the proposed public asset. It does not reopen the scientific freeze.

## Phase and QA Status

| Item | Status | Evidence |
|---|---|---|
| Phase 1 evidence freeze | PASS | Commit `bfee8f3`; compact release verifier passes with 46 bundle files and formal seeds `[0, 1, 2, 3, 4]`. |
| Phase 2 final figures | PASS | Commit `902497c`; six scientific figures and the method-design schematic were approved. |
| Phase 3 narrative | PASS | Commit `b7e51b2`; report, summary, README, captions, and release notes passed the frozen-number and claim-boundary checks. |
| Phase 4 clean-clone QA | PASS | Commit `601419d`; artifact-only verification, figure regeneration, and notebook execution passed without datasets or checkpoints. |
| Phase 5A full tests | PASS | 179 tests passed in 49.7 s. Two non-failing environment warnings were emitted by the existing representation test. |
| Phase 5A release-safe tests | PASS | 37 tests passed in 11.86 s inside a 314-file tracked/public export. |
| Notebook | PASS | The executed `project_demo.ipynb` validates and executes artifact-only in the clean export. |
| Figure regeneration | PASS | All 17 plotted-value tables regenerated byte-for-byte from compact frozen evidence. |
| Additional test access | PASS | Zero. No dataset, checkpoint, or model-evaluation path was used during wrap-up QA. |

No formal result, statistic, seed, scientific figure value, frozen protocol, or
Stage 2D/Stage 3 governance record was modified in Phase 5A.

Local machine paths were sanitized for public release without altering
experimental evidence or protocol content.

The public-facing documents, notebook, compact evidence, figures, and candidate
archive pass the private-path and secret-pattern scan. Two frozen historical
pytest logs under `verification/phase0_v1_1/` retain a non-user-specific local
workspace path as original log provenance. They contain no username,
credential, or secret and are excluded from the candidate evidence archive.

## Git Preflight

- Current branch: `codex/final-release`.
- Preflight source HEAD: `601419db6fc68854967d63fd6c6980e554dd9d27`.
- Configured origin: `https://github.com/elvisxtyao/neuroai-hebbian-vs-backprop-autoencoder.git`.
- The locally cached `origin/main` is `dfb6a3739e80a5885126a78c963c79f1eebece3f`.
- The merge base of the release branch and cached `origin/main` is that same
  commit. The release branch is a linear descendant and is 53 commits ahead;
  the cached remote ref has no commits absent from the release branch.
- No local `main` branch or `origin/HEAD` symbolic ref exists. The actual remote
  default branch and freshness of `origin/main` cannot be confirmed while
  authentication is invalid.
- The 53 release-only commits are related research, protocol, analysis,
  documentation, and release work. No unrelated project was found. The history
  does include related diagnostic/exploratory development and tracked
  `figures/presentation_stage1b/`; those are excluded from the compact release
  asset but would remain visible in a full repository-history merge.

Safest later merge path, after separate approval and blocker resolution:

1. Re-authenticate GitHub and fetch current remote refs without rewriting local
   history.
2. Confirm the remote default branch with `gh repo view` and compare it with
   `codex/final-release`.
3. If the release branch remains a descendant of the current default branch,
   push it and open a PR into that verified default branch.
4. Use a merge commit rather than squash or rebase so the formal protocol and
   governance history remains auditable. If new default-branch commits exist,
   review them before an explicit non-rewriting merge; do not rebase the frozen
   release automatically.
5. Fetch and verify the exact merged commit, rerun release-safe QA, then create
   the annotated `v1.0-final` tag on that verified commit.

No push, PR, merge, tag, or GitHub Release was performed in Phase 5A.

## GitHub State

Classification: **AUTH BLOCKED**.

`gh auth status` reports an active default account, `elvisxtyao`, but its token
is invalid. The read-only Git probe also fails with
`SEC_E_NO_CREDENTIALS (0x8009030E)`. General network access is available, but
repository existence, permissions, remote default branch, and current remote
refs cannot be verified through authenticated GitHub access.

Minimal user action: run `gh auth login -h github.com` interactively and grant
the account access to this repository. Then rerun `gh auth status`,
`gh repo view`, and a read-only remote-ref check. No proxy, credential-helper,
or global Git configuration change is recommended.

## License and Provenance Audit

License status: **PASS**. The repository owner's original code is released
under MIT. The narrowly adapted Neuromatch portion remains covered by the
upstream BSD 3-Clause conditions reproduced in `THIRD_PARTY_NOTICES.md`.

### Classification

| Class | Finding |
|---|---|
| A — clearly project-specific/original implementation record | Final analysis/release tooling, including `scripts/build_final_release.py`, `scripts/verify_final_release.py`, `scripts/plot_final_figures.py`, `scripts/qa_clean_export.py`, and `scripts/build_release_archive.py`; Stage 3 aggregation/analysis code; release-facing reports and figures. No external source mapping or third-party header was found for these files. |
| B — standard generated/config/test material | `configs/`, `tests/`, manifests, aggregate CSV/JSON, checksum files, environment/requirements records, split manifests, and verification output. Generated scientific values remain governed by their frozen provenance rather than a source-code license claim. |
| C — owner-authored Phase 0 BP/reference files | The repository owner confirms that `models/conv_autoencoder.py`, `models/linear_probe.py`, `learning_rules/__init__.py`, `learning_rules/backprop.py`, `training/train_representation.py`, and `training/train_linear_probe.py` were written entirely by the owner. They contain no teammate-authored source code and require no teammate redistribution or relicensing permission. |
| D — narrowly adapted BSD software | `learning_rules/hebbian.py` is independently implemented except for the optional output-filter centering operation. That operation adapts the official notebook's output-unit centering expression to Conv2d tensors; the upstream one-line expression is quoted in the local docstring. The full Neuromatch BSD notice is retained. |
| E — independently implemented from upstream concepts | `evaluation/update_analysis.py` and `evaluation/run_q4_tooling.py` use the general ideas of comparing learning-rule updates with backpropagation and measuring update variability. Their tensor operations, statistical definitions, frozen-snapshot protocol, integrity checks, and orchestration are independent; no Neuromatch code portion was copied into either file. |

The source comparison used the official Neuromatch Academy NeuroAI Course
`projects/project-notebooks/Microlearning.ipynb` at commit
`f8cdef10d7463ff626b8c6555a29a0fd918b9fd4`. The official repository states
that software elements are BSD 3-Clause licensed. The fetched source and
license SHA-256 values, local-source distinction, and comparison boundary are
recorded in `docs/history/tutorial_migration.md`.

No `HebbianFunction`, custom-autograd path, target-clamping path, upstream MLP,
training loop, plotting helper, or dataset code was copied into the three local
files. Exact-sequence comparison found only the documented one-line centering
expression in `learning_rules/hebbian.py`; generic idioms such as
`loss.backward()` followed by `optimizer.step()` were not treated as
source-derived implementations.

### License conclusions

1. The owner's original repository code can be released under MIT.
2. BSD 3-Clause is permissive and compatible with distribution of the combined
   repository under MIT, provided the Neuromatch copyright, three conditions,
   disclaimer, and non-endorsement boundary remain with the adapted portion.
3. `THIRD_PARTY_NOTICES.md` retains those terms and identifies the exact local
   scope. The main `LICENSE` does not purport to relicense Neuromatch's portion.
4. No upstream notebook is tracked, included in `release/v1.0-final/`, or
   included in the proposed evidence ZIP.
5. No teammate ownership or redistribution permission is required for the six
   owner-authored Phase 0 files. Pending teammate protocol confirmation remains
   archival and is not a license blocker.

## Attribution Assessment

- Course context and the official Neuromatch source are identified without
  implying endorsement.
- The required Neuromatch copyright, conditions, and disclaimer are reproduced
  in `THIRD_PARTY_NOTICES.md`.
- No teammate code contribution is attributed because the owner confirms the
  six Phase 0 destination files are entirely owner-authored.
- Pending Phase 0 team confirmation concerns protocol compliance only and is
  not an ownership, relicensing, or redistribution issue.

## Candidate Release Asset

Candidate name: `v1.0-final-evidence.zip`.

The deterministic allowlist contains exactly 86 files:

- all 48 files under `release/v1.0-final/`, including 46 verified bundle
  members, the checksum list, and release notes;
- all 32 files under `figures/final/`, including six PNG/PDF figure pairs, the
  method-design pair, 17 plotted-value tables, and `source_manifest.json`;
- `FINAL_REPORT.md`;
- `LICENSE`;
- `RESULTS_SUMMARY.md`;
- `REPRODUCIBILITY.md`;
- `THIRD_PARTY_NOTICES.md`;
- the executed artifact-only `project_demo.ipynb`.

The tested candidate is 3,958,816 bytes (approximately 3.96 MB decimal). The
builder fixes member order, timestamps, Unix file modes, archive root, and
compression level; two independent temporary builds were byte-identical. It
refuses output overwrite and rejects results, recovery, representation,
checkpoint, log, exploratory-presentation, tutorial, and model-binary paths.

Excluded content includes raw datasets, the ignored `results/` archive,
checkpoints, embeddings/representation arrays, update tensors, recovery or
partial outputs, local logs, preliminary figures, credentials, all source
notebooks, and all training/model/evaluation source code. The archive contains
no upstream notebook or source implementation; it includes the required BSD
notice and the repository's MIT license.

No persistent candidate ZIP or final sidecar checksum was created because
GitHub preflight remains blocked. Run the deterministic builder with
`--write-checksum` only after the candidate contents receive final approval.

## Remaining Blockers

1. **GitHub authentication/remote verification:** the active `gh` token is
   invalid, so remote permissions, default branch, and ref freshness are
   unverified.
The license and redistribution blocker is resolved. Phase 5B must not begin
until GitHub authentication and remote verification pass and the release
decision is reviewed.
